# Claude Autopilot Plugin

Autonomous task execution mirror of Copilot's Autopilot.

## Usage

Run `/autopilot "your task"` to start an autonomous loop.
The agent will manage its own tasks via a local SQLite database and exit when finished.
